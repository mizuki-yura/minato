
# if / else

条件によって処理を分岐します。

## 基本

```
OnBoot => {
    if (save.訪問回数 == 0) {
        湊: はじめまして。
    } else {
        湊: また来てくれたんですね。
    }
}
```

## else if

```
OnBoot => {
    if (save.好感度 >= 100) {
        湊: 大好きです。
    } else if (save.好感度 >= 50) {
        湊: 好きです。
    } else {
        湊: ……。
    }
}
```

## 比較演算子

| 演算子 | 意味 |
|---|---|
| `==` | 等しい |
| `!=` | 等しくない |
| `<` | より小さい |
| `<=` | 以下 |
| `>` | より大きい |
| `>=` | 以上 |

## 論理演算子

| 演算子 | 意味 |
|---|---|
| `&&` | かつ |
| `\|\|` | または |
| `!` | 否定 |

```
OnBoot => {
    if (save.訪問回数 >= 10 && save.好感度 >= 50) {
        湊: たくさん来てくれていますね。
    }
}
```

## elseなし

`else` は省略できます。

```
OnBoot => {
    if (save.訪問回数 == 0) {
        global save.初回フラグ = true
    }
    湊: こんにちは。
}
```